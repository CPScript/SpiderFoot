run spiderfoot safely using tor on your linux computer (easy to use launcher/installer)

* clone repo: `git clone https://github.com/CPScript/SFEasy`
* go to repo's dir: `cd SFEasy`
* run script: `bash run.sh`
